package org.lx.cs.server.util;

public class SessionUtil {

//    public static boolean saveSessionToChannel(Channel channel, ChannelType channelType) {
//        Session session = new Session();
//        session.setChannel(channel);
//        session.setChannelType(channelType);
//        session.setIp(ChannelUtil.getChannelIP(channel));
//        return channel.attr(Session.SESSION_KEY).compareAndSet(null, session);
//    }
//
//    public static Session getSessionFromChannel(Channel channel) {
//        return channel.attr(Session.SESSION_KEY).get();
//    }
//
//    public static void removeSessionFromChannel(Channel channel) {
//        channel.attr(Session.SESSION_KEY).set(null);
//    }

}
